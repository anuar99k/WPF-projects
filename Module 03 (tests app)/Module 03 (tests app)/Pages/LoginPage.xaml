<Page x:Class="Module_03__tests_app_.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Module_03__tests_app_.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginPage">

    <StackPanel HorizontalAlignment="Center"
          VerticalAlignment="Center" x:Name="spWrapper">

        <StackPanel Orientation="Horizontal" Margin="10">
            <Label Content="Логин: " FontSize="16"
                   Margin="0 0 10 0"/>
            <TextBox x:Name="tbxLogin"
                     FontSize="16" MinWidth="150"
                     Text="{Binding Path=Login}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="10">
            <Label Content="Пароль: " FontSize="16"/>
            <PasswordBox x:Name="psbPassword"
                     FontSize="16" MinWidth="150"/>
        </StackPanel>

        <Button Content="Войти" FontSize="16"
                Margin="0 10 0 0" Width="150"
                Padding="3" x:Name="btnSignIn"
                Click="BtnSignIn_Click"/>

    </StackPanel>
</Page>
